CREATE TABLE [dbo].[Tamagotchi]
(
	[Name] VARCHAR(50) NOT NULL PRIMARY KEY, 
    [Age] INT NOT NULL, 
    [Coins] INT NOT NULL, 
    [Level] INT NOT NULL, 
    [Health] INT NOT NULL, 
    [Boredom] INT NOT NULL 
)
